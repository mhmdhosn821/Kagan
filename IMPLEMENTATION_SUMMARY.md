# مستند تغییرات سیستم Kagan ERP

## خلاصه تغییرات

این مستند شامل تمام تغییرات اعمال شده بر روی سیستم Kagan ERP برای بهبود رابط کاربری و افزودن قابلیت‌های جدید است.

## ✅ تغییرات انجام شده

### 1. طراحی مجدد صفحه ورود (Login Page) با Glass Morphism

**فایل:** `kagan_desktop/ui/login.py`

**تغییرات اعمال شده:**
- پس‌زمینه گرادیانت بنفش-آبی زیبا
- فریم ورود با افکت Glass Morphism (شفافیت و بلور)
- افزایش اندازه فونت‌ها:
  - عنوان: 24px (bold)
  - زیرعنوان: 13px
  - فیلدها: 12px
  - دکمه: 14px (bold)
- ارتفاع فیلدهای ورودی: 50px
- دکمه ورود با گرادیانت آبی روشن
- افکت‌های hover و press روی دکمه‌ها

### 2. منوی تاشو سه‌گانه در پنجره اصلی (Main Window)

**فایل:** `kagan_desktop/ui/main_window.py`

**تغییرات اعمال شده:**
- ایجاد کلاس `CollapsibleMenu` برای منوهای تاشو
- تقسیم منو به سه بخش اصلی:
  - **💇 آرایشگاه:** داشبورد، خدمات، آرایشگران، نوبتدهی، فاکتور، گزارش عملکرد
  - **☕ کافهبار:** داشبورد، محصولات، باریستاها، فاکتور، دستور ساخت
  - **📦 انبار:** انبار کافه، انبار آرایشگاه، هشدار موجودی، سفارش خرید
- سایدبار با افکت Glass Morphism (rgba با blur)
- افزایش اندازه فونت منو:
  - منوی اصلی: 15px (font-weight: 600)
  - زیرمنو: 13px
  - عنوان صفحه: 18px (bold)
- ScrollArea برای منو جهت نمایش بهتر محتوا
- دکمه خروج با رنگ قرمز و استایل متمایز

### 3. صفحات مدیریت جدید

#### 3.1 مدیریت آرایشگران
**فایل:** `kagan_desktop/ui/barbers.py`

**قابلیت‌ها:**
- لیست آرایشگران با جستجو
- افزودن آرایشگر جدید
- ویرایش اطلاعات آرایشگر
- فیلدها: نام، نام کاربری، شماره تماس، تخصص، درصد کمیسیون
- تغییر وضعیت فعال/غیرفعال
- دکمه‌های واضح با آیکون و متن

#### 3.2 مدیریت باریستاها
**فایل:** `kagan_desktop/ui/baristas.py`

**قابلیت‌ها:**
- لیست باریستاها با جستجو
- افزودن باریستا جدید
- ویرایش اطلاعات باریستا
- فیلدها: نام، نام کاربری، شماره تماس، شیفت کاری
- تغییر وضعیت فعال/غیرفعال
- دکمه‌های واضح با آیکون و متن

#### 3.3 گزارش عملکرد آرایشگران
**فایل:** `kagan_desktop/ui/barber_report.py`

**قابلیت‌ها:**
- فیلتر بر اساس بازه زمانی
- نمایش تعداد خدمات انجام شده
- محاسبه درآمد کل
- محاسبه کمیسیون دریافتی
- مرتب‌سازی بر اساس درآمد

### 4. بهبود صفحه انبار (Inventory Page)

**فایل:** `kagan_desktop/ui/inventory.py`

**تغییرات اعمال شده:**
- ScrollArea برای نمایش بهتر محتوا
- دکمه‌های بزرگ و واضح:
  - ➕ افزودن کالا (primaryButton)
  - 📦 افزودن موجودی (successButton)
  - ⚠️ هشدارها (warningButton)
- دکمه‌های عملیات در جدول با متن و آیکون:
  - 📦 موجودی (successButton - حداقل 80px)
  - ✏️ ویرایش (infoButton - حداقل 80px)
- فیلد جستجو با ارتفاع 40px
- متد `show_add_stock_dialog()` برای نمایش کالاهای کم‌موجود
- متد `show_alerts()` برای نمایش هشدارهای موجودی
- نمایش موجودی با رنگ قرمز برای کالاهای کم‌موجود

### 5. دستور ساخت چند مادهای (Multi-Ingredient Recipe)

**فایل:** `kagan_desktop/ui/cafe.py`

**قابلیت‌ها:**
- دکمه "📝 دستور" در کنار هر محصول
- دیالوگ مدیریت دستور ساخت (`RecipeDialog`)
- افزودن چندین ماده اولیه به هر محصول
- نمایش موجودی فعلی هر ماده
- هشدار رنگی برای مواد کم‌موجود
- دیالوگ افزودن ماده (`AddIngredientDialog`)
- انتخاب ماده از انبار کافه
- تعیین مقدار دقیق برای هر ماده
- جلوگیری از افزودن مواد تکراری

### 6. تغییرات پایگاه داده

**فایل:** `kagan_desktop/database.py`

**تغییرات schema:**
- افزودن فیلد `phone` به جدول users
- افزودن فیلد `specialty` به جدول users (برای آرایشگران)
- افزودن فیلد `shift` به جدول users (برای باریستاها)
- جدول `product_recipe` از قبل وجود داشت و آماده استفاده است

### 7. استایل Glass Morphism جامع

**فایل:** `kagan_desktop/assets/styles.qss`

**تغییرات استایل:**

#### پس‌زمینه
- گرادیانت نرم از #f5f7fa به #c3cfe2

#### دکمه‌ها
- **پیش‌فرض:** گرادیانت آبی (#4facfe → #00f2fe)
- **Primary:** گرادیانت بنفش (#667eea → #764ba2)
- **Success:** گرادیانت سبز (#11998e → #38ef7d)
- **Info:** گرادیانت آبی روشن (#4facfe → #00f2fe)
- **Warning:** گرادیانت صورتی-قرمز (#f093fb → #f5576c)
- **Danger:** گرادیانت قرمز (#ee5a6f → #f29263)
- اندازه فونت: 13px، font-weight: 500
- حداقل ارتفاع: 35px
- border-radius: 8px
- padding: 10px 20px

#### فیلدهای ورودی
- پس‌زمینه: rgba(255, 255, 255, 0.8)
- border: 2px solid rgba(255, 255, 255, 0.3)
- border-radius: 8px
- padding: 8px 12px
- اندازه فونت: 13px
- در حالت focus:
  - پس‌زمینه: rgba(255, 255, 255, 0.95)
  - border: 2px solid #4facfe

## 📊 آمار تغییرات

- **فایل‌های تغییر یافته:** 5 فایل
- **فایل‌های جدید:** 3 فایل
- **کل تغییرات:** 8 فایل

### فایل‌های تغییر یافته (Modified)
1. `kagan_desktop/assets/styles.qss` - استایل‌های Glass Morphism
2. `kagan_desktop/database.py` - افزودن فیلدهای جدید
3. `kagan_desktop/ui/login.py` - طراحی مجدد با Glass Morphism
4. `kagan_desktop/ui/main_window.py` - منوی تاشو سه‌گانه
5. `kagan_desktop/ui/inventory.py` - بهبود دکمه‌ها و قابلیت‌ها
6. `kagan_desktop/ui/cafe.py` - دستور ساخت چند مادهای

### فایل‌های جدید (Added)
1. `kagan_desktop/ui/barbers.py` - مدیریت آرایشگران
2. `kagan_desktop/ui/baristas.py` - مدیریت باریستاها
3. `kagan_desktop/ui/barber_report.py` - گزارش عملکرد

## 🎨 راهنمای استفاده از استایل‌ها

### استفاده از دکمه‌های رنگی

```python
# دکمه اصلی (بنفش)
btn = QPushButton("ذخیره")
btn.setObjectName("primaryButton")

# دکمه موفقیت (سبز)
btn = QPushButton("افزودن")
btn.setObjectName("successButton")

# دکمه اطلاعات (آبی)
btn = QPushButton("مشاهده")
btn.setObjectName("infoButton")

# دکمه هشدار (صورتی-قرمز)
btn = QPushButton("هشدار")
btn.setObjectName("warningButton")

# دکمه خطر (قرمز)
btn = QPushButton("حذف")
btn.setObjectName("dangerButton")
```

## 🔄 نحوه اجرا و تست

### پیش‌نیازها
```bash
pip install PyQt6 PyQt6-Charts
```

### اجرای برنامه
```bash
cd kagan_desktop
python main.py
```

### کاربران پیش‌فرض
- **مدیر:** admin / admin123
- **آرایشگر:** barber1 / barber123
- **باریستا:** barista1 / barista123

## 📋 چک‌لیست ویژگی‌های پیاده‌سازی شده

- ✅ صفحه لاگین با Glass Morphism
- ✅ منوی سه‌گانه با زیرمنوهای تاشو
- ✅ صفحات مدیریت آرایشگران و باریستاها
- ✅ گزارش عملکرد آرایشگران
- ✅ بهبود صفحه انبار با دکمه‌های واضح
- ✅ دستور ساخت چند مادهای برای محصولات کافه
- ✅ استایل Glass Morphism در کل برنامه
- ✅ فونت‌های درشت‌تر (15px منو، 13px زیرمنو)
- ✅ ScrollArea در صفحات
- ✅ دکمه‌ها با آیکون و متن

## 📝 نکات مهم

1. **RTL:** تمام صفحات به صورت RTL (راست به چپ) هستند
2. **فونت:** از فونت وزیر استفاده می‌شود
3. **سازگاری:** با PyQt6 سازگار است
4. **پایگاه داده:** SQLite با schema بروزرسانی شده
5. **امنیت:** پسوردها با SHA256 هش می‌شوند

## 🎯 ویژگی‌های باقی‌مانده (برای آینده)

- ⏳ فاکتورزنی حرفهای با چاپ و PDF
- ⏳ QR Code در فاکتورها
- ⏳ کسر خودکار موجودی بر اساس دستور ساخت
- ⏳ نمودار مقایسهای در گزارش عملکرد
- ⏳ پیاده‌سازی کامل صفحه سفارش خرید

## 🔧 نکات فنی برای توسعه‌دهندگان

### افزودن صفحه جدید
1. فایل جدید در `ui/` بسازید
2. کلاس صفحه باید از `QWidget` ارث‌بری کند
3. در `main_window.py` import کنید
4. به `pages` در متد `load_pages()` اضافه کنید
5. شماره صفحه را در `switch_page()` اضافه کنید

### افزودن دکمه به منو
در متد `create_sidebar()` در `main_window.py`:
```python
btn = QPushButton("🔰 عنوان منو")
btn.setObjectName("sidebarButton")
btn.clicked.connect(lambda: self.switch_page(شماره_صفحه))
```

### افزودن زیرمنو
```python
items = [
    ("عنوان 1", شماره_صفحه_1),
    ("عنوان 2", شماره_صفحه_2),
]
menu = CollapsibleMenu("نام منو", "🔰", items)
menu.menu_clicked.connect(self.switch_page)
```

---

**نسخه:** 2.0.0  
**تاریخ:** ۱۴۰۴/۱۰/۱۴  
**توسعه‌دهنده:** Kagan ERP Team  
